# 监测页面产品需求文档

**版本：** 0.5
**页面：** 监测页（SummaryScreen）
**状态：** 待评审
**最后更新：** 2026-07-27

---

## 一、页面整体结构

页面分为两个区域：

- **固定 Header**：不随内容滚动，包含设备胶囊、蓝牙状态行、右上角客服入口
- **可滚动内容区**：场景标题、健康评分圆环、数据同步卡片、AI 分析流程、血压健康指标、心血管指标

---

## 二、Header 区域

**胶囊圆点含义：**

| 颜色 | 含义 |
|---|---|
| 🟢 绿色 | 当前选中设备已被蓝牙搜索到 |
| ⚫ 灰色 | 当前选中设备未被搜索到 / 不在范围内 |

---

**展开设备列表规则：**

列表显示所有当前已被蓝牙搜索到的设备。每台设备的显示规则如下：

| 情况 | 圆点 | 状态标签 |
|---|---|---|
| 已搜索到 + 当前选中连接中 | 🟢 绿色 | "已连接" |
| 已搜索到 + 未选中 | 🟢 绿色 | 无标签 |
| 历史绑定 + 当前未搜索到 | ⚫ 灰色 | 无标签 |

举例：蓝牙范围内有 3 台设备广播运行，3 台圆点均为绿色，但只有选中的那台后面显示"已连接"。

**切换设备：**
- 点击列表中任意一台绿点设备 → App 断开当前连接，向目标设备发起连接
- 连接成功后立即开始同步数据，无需等待下次测量
- 连接成功后目标设备显示"已连接"，原设备标签消失

**删除设备：**
- 每行右侧有"删除"按钮
- 点击后需二次确认弹框，防止误删
- 删除的是绑定关系，不是黑名单。删除后该设备仍可被搜索到并重新绑定
- 删除后若该设备仍在蓝牙范围内，下次搜索依然会出现在列表中

**添加设备：**
- 列表底部固定显示"添加设备"入口
- 点击跳转至扫描二维码页面，扫描设备背面二维码完成绑定
- 与蓝牙自动搜索是两套独立流程

---

### 2.2 蓝牙自动搜索与状态行

#### 触发时机与优先级

**这是页面最高优先级任务。** 以下情况立即自动触发蓝牙搜索：

1. App 冷启动，首次进入监测页
2. 用户从其他页面返回监测页
3. 用户在监测页下拉刷新

进入监测页后蓝牙搜索立即开始，无需用户任何操作。

---

#### 搜索与重试机制

**单次搜索上限：15 秒**

- 15 秒内找到设备 → 立即进入连接流程，计时终止
- 15 秒未找到设备 → 显示失败状态 3 秒 → 自动触发下一轮搜索

**重试循环规则：**

只要停留在监测页且未成功连接设备，系统持续按以下节奏循环：

```
搜索 15 秒
    │
    ├─ 找到设备 ──► 连接成功，循环终止
    │
    └─ 未找到 ──► 显示失败状态 3 秒 ──► 自动重新搜索 15 秒
                        └─────────────────────────┘
                            持续循环，直到连接成功
                            或用户离开监测页
```

**手动触发：**
- 用户点击"去搜索 →" → 立即重置计时，重新发起一轮 15 秒搜索，不等待 3 秒间隔
- 手动触发后若仍未找到，继续每 3 秒自动重试

**停止条件：**

| 条件 | 行为 |
|---|---|
| 连接设备成功 | 循环终止，进入数据同步流程 |
| 用户离开监测页 | 循环终止，返回后重新从第一轮开始 |

---

#### 各状态显示规则

| 状态 | 触发条件 | 显示内容 |
|---|---|---|
| 搜索中 | 每轮搜索进行中 | 旋转图标 + "正在搜索设备…" |
| 同步成功 | 找到设备，有新血压数据 | ✅ + "N 条血压数据同步成功 HH:MM" |
| 无新数据 | 找到设备，无新数据 | 蓝牙图标 + "设备已连接，暂无新数据" |
| 连接失败 | 未找到设备，手机蓝牙未开启 | ⚠ + "设备连接失败" + "去连接 →" |
| 暂未搜到设备 | 本轮 15 秒超时，蓝牙已开但无响应 | 灰图标 + "暂未搜到设备" + "去搜索 →" |

**蓝牙被关闭的实时检测：**
- 用户在系统设置关闭蓝牙后，返回监测页或在监测页下拉时实时检测
- 检测到蓝牙已关闭，直接切换为"连接失败"状态，显示"去连接 →"按钮
- 不弹 Toast 提示，状态行已承载完整信息

---

#### 首次使用引导（无绑定设备）

新用户首次进入监测页，没有任何绑定设备时：
- 系统仍正常执行 15 秒蓝牙搜索
- 若搜索到附近有血压设备广播 → 页面底部自动弹出设备发现弹框，引导用户绑定
- 若未搜索到任何设备 → 状态行显示"暂未搜到设备"，进入常规重试循环

---

### 2.3 客服入口（右上角）

- 图标样式：粉红色爱心，内嵌白色问号
- 点击后跳转至客服帮助页面

---

## 三、数据同步规则

### 3.1 同步优先级

- 同一时刻只同步当前选中的已连接设备数据
- 列表中其他绿点设备（已搜索到但未选中）不做后台静默同步

### 3.2 历史未同步数据的处理

- 每次连接成功后，App 与设备比对数据，识别上次同步后的新增记录
- 若上次同步中途失败，本次连接重新比对，补传所有未同步成功的数据
- 比对基准：以**时间戳**为唯一标识进行去重，防止重复上传

### 3.3 数据同步失败处理

- 同步过程中蓝牙或网络断开 → 本次传输整体视为失败
- 设备标记本次传输失败，下次连接时重新上传
- 状态行本期不展示"同步失败"的明确提示，失败后按正常搜索重试流程处理

### 3.4 后台保持连接

- 用户切换到其他 Tab 或其他页面，蓝牙连接保持不断开
- 若设备在后台完成数据上传，用户返回监测页后页面自动刷新，展示最新同步结果
- 用户返回时若动画已播完、AI 分析已完成，直接显示最终结果，不重播动画

### 3.5 设备固件重置处理

- 设备恢复出厂后 MAC 地址不变，App 以 MAC 地址识别设备身份
- 视为同一台设备，绑定关系保留，重置后重新连接不需要重新绑定

---

## 四、场景化标题

位于健康评分圆环左上方，根据当前时段动态切换，默认显示"血压健康"：

| 时段 | 标题文案 |
|---|---|
| 默认 | 血压健康（大标题） |
| 早晨 | ☀️ 早上好，记得测量清晨血压 |
| 夜间 | 🌙 夜深了，早点睡稳住血压 |

时段判断基于本地时间，具体时间阈值由研发确认。

---

## 五、血压健康评分圆环

### 5.3 评分规则说明弹框

- "近 7 日健康评分"后有灰色小问号
- 点击从页面底部滑出弹框，说明评分打分规则
- 支持下滑关闭，不覆盖 Header

---

## 六、数据同步卡片

### 6.1 同步条数提示

| 情况 | 显示文案 |
|---|---|
| 同步 1 条 | "1 条血压数据同步成功 HH:MM" |
| 同步多条 | "N 条血压数据同步成功 HH:MM" |

### 6.2 血压数值展示

- 展示本次同步的最后一笔数据
- 副标题："最后一笔 MM.DD HH:MM"
- 高压 / 低压数值大字显示，单位 mmHg
- 多笔数据时高压和低压播放数字滚动动画，从第 1 条滚动至最后一条后停止
- 单条数据直接显示，无动画

### 6.3 脉搏数据

- 位于血压数值右侧，爱心图标 + 数值 + "次/分"
- 来源：设备本次上传的脉搏字段

### 6.4 异常标识

| 标识 | 展示条件 |
|---|---|
| 房颤 | 设备上传数据携带房颤标识 |
| 不规则脉搏 | 设备上传数据携带不规则脉搏标识 |

设备未上传对应字段则不展示，仅反映本次数据，不做历史累计。

### 6.5 心血管风险入口

- 血压数值右侧显示"发现 N 项心血管风险 ›"
- 点击跳转心血管风险详情页

---

## 七、AI 分析流程

### 7.1 正常流程

- 最后一笔数据同步完成后自动启动
- 多个步骤依次呈现，每步完成后左侧出现 ✅
- 全部完成后出现"查看分析结果"按钮，点击跳转 AI 报告页

### 7.2 失败重试

- 分析中断后右侧出现"重新生成"按钮
- 点击后清空步骤状态，重新发起完整分析调用
- 步骤动画从头依次呈现

### 7.3 用户中途离页

- 用户离开时 AI 分析在后台继续运行
- 用户返回监测页后直接显示最终结果，不重播分析过程动画

---

## 八、本文档范围外（待后续补充）

- 血压健康指标模块
- 心血管健康指标模块
- AI 健康助手对话卡片
- 动态血压入口按钮
- 连续测量积分 / 权益解锁模块
